services:
  databaseengine:
    image: databaseengine
    build:
      context: .
      dockerfile: ./dbcsv/engine/Dockerfile
    ports:
      - 80:80

  dbapi2:
    image: dbapi2
    build:
      context: .
      dockerfile: ./dbcsv/dbapi2/Dockerfile
